# --- Scheduling Configuration ---
# Date of the pass you want to book (format: YYYY-MM-DD)
TARGET_DATE=2024-06-18
# Whether to use scheduling (true/false). If true, the script will wait for the release time for TARGET_DATE.
SCHEDULE=true

# Path to your Chrome user data directory (absolute path or relative to this repo, will be created if it doesn't exist)
# Default: chrome-profile (in this repo)
USER_DATA_DIR=chrome-profile

# If left blank, the default is chrome-profile in this repo.

# --- Booking Configuration ---
# Unique keyword or phrase to identify your vehicle (e.g., 'Tesla', 'MV763F', etc.)
VEHICLE_KEYWORD=Tesla

# URL for the All Day Pass page
ALL_DAY_PASS_URL=https://your-all-day-pass-url.com

# URL for the Half Day Pass page
HALF_DAY_PASS_URL=https://your-half-day-pass-url.com

# --- Optional: Polling Configuration ---
# Time to switch from slow polling (every 1s) to fast polling (every 0.05s) before START_TIME (24-hour format, e.g., 06:59)
SLOW_POLL_UNTIL=06:59

# Time to start the booking flow (24-hour format, e.g., 07:00)
START_TIME=07:00

# Day of the week to run (e.g., Monday, Tuesday, etc.)
DAY_OF_WEEK=Monday

# --- Pass Type Selection ---
# Whether to check for All Day Pass (true/false)
CHECK_ALL_DAY=true

# Whether to check for Morning Pass (true/false)
CHECK_MORNING=false

# Whether to check for Afternoon Pass (true/false)
CHECK_AFTERNOON=false