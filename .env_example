# Path to your Chrome user data directory (e.g., /home/youruser/.config/google-chrome)
# Mac Default: /Users/<username>/Library/Application Support/Google/Chrome

# Run this command in a terminal
#    open ~/Library/Application\ Support/Google/Chrome
# Then use the path bar on the bottom of finder, right click Chrome and "Copy Chrome as Pathname"

# Windows Default: C:\Users\<username>\AppData\Local\Google\Chrome\User Data\Default
# Run this command in a terminal
#    open C:\Users\<username>\AppData\Local\Google\Chrome\User Data\
# Then use the path bar on the bottom of finder, right click Chrome and "Copy Chrome as Pathname"

# Linux Default: ~/.config/google-chrome

# Run this command in a terminal
# Linux Default: ~/.config/google-chrome

# Run this command in a terminal
#    open ~/.config/google-chrome
# Then use the path bar on the bottom of finder, right click Chrome and "Copy Chrome as Pathname"

# Chrome profile directory (e.g., "Default" or "Profile 1")
PROFILE_DIRECTORY=Default

# The user data directory is auto-detected for your platform unless override is enabled below.
# Optional: Override the Chrome user data directory
USER_DATA_DIR_OVERRIDE_ENABLED=false
USER_DATA_DIR_OVERRIDE=

# Unique keyword or phrase to identify your vehicle (e.g., 'Tesla', 'MV763F', etc.)
VEHICLE_KEYWORD=Tesla

# URL for the All Day Pass page
ALL_DAY_PASS_URL=https://your-all-day-pass-url.com

# URL for the Half Day Pass page
HALF_DAY_PASS_URL=https://your-half-day-pass-url.com

# Whether to use scheduling (true/false)
SCHEDULE=false

# Time to wake up (24-hour format, e.g., 06:55)
WAKEUP_TIME=06:55

# Time to start the script (24-hour format, e.g., 07:00)
START_TIME=07:00

# Day of the week to run (e.g., Monday, Tuesday, etc.)
DAY_OF_WEEK=Monday

# Whether to check for All Day Pass (true/false)
CHECK_ALL_DAY=true

# Whether to check for Morning Pass (true/false)
CHECK_MORNING=false

# Whether to check for Afternoon Pass (true/false)
CHECK_AFTERNOON=false