[project]
name = "buntzen-pass-bot"
version = "0.1.0"
description = "Unattended Playwright bot for Buntzen Lake Yodel pass booking."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.139.0",
    "jinja2>=3.1",
    "playwright>=1.61.0",
    "python-dotenv>=1.0",
    "twilio>=9.0",
    "uvicorn[standard]>=0.50.2",
]

[tool.uv]
package = false
