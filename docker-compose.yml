services:
  buntzen-pass-bot:
    build: .
    container_name: buntzen-pass-bot
    restart: unless-stopped
    init: true
    ports:
      - "${WEB_PORT:-8080}:8080"
    environment:
      APPDATA_DIR: /appdata
      BUNTZEN_ADMIN_USERNAME: "${BUNTZEN_ADMIN_USERNAME:-admin}"
      BUNTZEN_ADMIN_PASSWORD: "${BUNTZEN_ADMIN_PASSWORD:-}"
      BLUEBUBBLES_URL: "${BLUEBUBBLES_URL:-http://bluebubbles.example:1234}"
      BUNTZEN_ALLOWED_ORIGINS: "${BUNTZEN_ALLOWED_ORIGINS:-}"
      MAX_CONCURRENT_JOBS: "${MAX_CONCURRENT_JOBS:-2}"
      SCHEDULES_ENABLED: "${SCHEDULES_ENABLED:-false}"
    volumes:
      - ./appdata:/appdata
    shm_size: "1gb"
    security_opt:
      - "seccomp=./docker/seccomp_profile.json"
    stop_grace_period: 45s
